const songDetails = function(album) {
    const songDetails = document.querySelector(".details");
    songDetails.innerHTML = "";
    const songDetailsHeader = document.createElement("h2");
    songDetailsHeader.innerText = `About: ${song.songName}`;

}